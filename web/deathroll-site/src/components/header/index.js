import { h } from 'preact';
import { Link } from 'preact-router/match';
import style from './style.css';

const Header = () => (
  <header class="masthead mb-auto">
    <h3 class="masthead-brand"><a href="/">Death Roll</a></h3>
  </header>
);

export default Header;
